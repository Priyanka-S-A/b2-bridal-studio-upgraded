{
    "name": "beauty-parlor-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "npx -y concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install --prefix backend && npm install --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "@react-oauth/google": "^0.13.5",
        "google-auth-library": "^10.6.2"
    }
}
